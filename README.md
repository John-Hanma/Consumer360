# Consumer360
customer segmentation using RFM analysis.

### Raw Data Location

The raw retail CSV is expected to be placed in the `data/` directory:

data/Sales Transaction v.4a.csv

For local execution, the ingestion owner copies this file to a system-accessible
location (e.g. `/tmp`) and updates the file path at runtime.
 
